<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sense.tutorial">

    <uses-permission android:event_name="android.permission.INTERNET" />
    <uses-permission android:event_name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:event_name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:event_name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:event_name="android.permission.CAMERA" />
    <uses-permission android:event_name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:event_name=".MainActivity">
            <intent-filter>
                <action android:event_name="android.intent.action.MAIN" />

                <category android:event_name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:event_name=".MembersListActivity.MembersListActivity"></activity>
    </application>

</manifest>